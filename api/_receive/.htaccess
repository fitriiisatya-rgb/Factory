# This portal is PUBLIC BY DESIGN (access control is the high-entropy
# ?token= query parameter, not a login) — Options -Indexes here only stops
# directory listing from leaking filenames, nothing more is needed.
Options -Indexes
